{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "_site"
        }
      }
    ],
    "buildCommand": "npm install && cd site && npm run install-jekyll && cd .. && npm start"
  }
  